import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Toaster } from "@/components/ui/toaster"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Wavelaunch Studio Application",
  description: "Apply to build your brand with Wavelaunch Studio - A brand-building accelerator for creators and influencers",
  keywords: "wavelaunch, creator, influencer, brand building, D2C, business accelerator",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  )
}

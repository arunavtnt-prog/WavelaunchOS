// This schema links to the shared CRM database
// All models are defined in the main CRM schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
